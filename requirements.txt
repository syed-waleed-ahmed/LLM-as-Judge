# Runtime + UI dependencies.
#
# pyproject.toml is the single source of truth for versions; this file simply
# installs the project with its "ui" extra so `pip install -r requirements.txt`
# keeps working for users who prefer it.
.[ui]
